<h2>All Tasks</h2>
<% @tasks.each do |task| %>
  <p>
<%= task.name %>
<%= link_to 'Update',edit_category_task_path(@category,task) %>
<%= link_to 'Delete',category_task_path(@category,task),method: :delete,data:{confirm:"Really?"} %>
<% end %>
